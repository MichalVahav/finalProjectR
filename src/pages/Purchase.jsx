import React from 'react'

function Purchase() {
    // const customers= useSelector((state)=>state.customers);  
    // const products= useSelector((state)=>state.products);  
    // const purchases= useSelector((state)=>state.purchases);  
    // const [customerData, setCustomerData] = useState([]);
    // useEffect(()=>{
    //  const filteredPurchases =purchases.filter(((purchase)=>purchase.productID===product.product.id))
    //  console.log('1',filteredPurchases);
    //   const extractedData=filteredPurchases.map((purchase)=>{
    //   const customer= customers.find((cus)=>cus.id===purchase.customerID)
    //      return{
    //        purchase, 
    //        customer
    //      }
    //    })
    //    setCustomerData(extractedData)
    //  },[purchases,customers])
   
  

    return (
    <div>
        
        Purchase</div>
  )
}

export default Purchase